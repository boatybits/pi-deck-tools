# pi-deck-tools dependencies
# Install on Pi: pip install -r requirements.txt

# Astronomy / celestial navigation
skyfield>=1.48

# PDF generation (sun/moon report)
reportlab>=4.0

# Plotting (used by sun/moon tool)
matplotlib>=3.7

# Data handling (Skyfield Hipparcos catalog loading)
pandas>=2.0

# HTTP (Signal K REST client)
requests>=2.31

# Image handling
Pillow>=10.0

# tkinter is part of Python stdlib — ensure python3-tk is installed on the Pi:
# sudo apt install python3-tk
